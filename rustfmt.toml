# Trelis Rust formatting configuration
edition = "2024"
max_width = 100
tab_spaces = 4
newline_style = "Unix"
use_small_heuristics = "Default"

# Imports (stable options only)
reorder_imports = true

# Function formatting (stable options only)
fn_params_layout = "Tall"
